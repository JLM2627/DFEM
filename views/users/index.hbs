<div>
    <h1>Desserts for Every Mood!
    </h1>
    <div>
        <a href="/users/new">Are you a dessert lover?!?!</a>
    </div>

    {{#each users}}
        <div>
            {{this.name}} - {{this.email}}
            <a href="/users/{{this._id}}">{{this.name}}'s Info</a>
            <a href="/users/{{this._id}}/">{{this.name}}'s Recipes</a>
        </div>
    {{/each}}

  
</div>